steps:
  - label: build and push container
    depends_on: ~
    commands:
      - docker build -t ghcr.io/seankhliao/webserve .
      - docker push ghcr.io/seankhliao/webserve
      - COSIGN_PASSWORD= cosign sign --key ~/.ssh/cosign.key ghcr.io/seankhliao/webserve
